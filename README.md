# Web Rating System - Fullstack Portfolio
## 📖 Project Overview

**Web Rating System** adalah aplikasi web berbasis Python (Flask) yang memungkinkan pengguna memberikan penilaian (rating) dan ulasan. Proyek ini mendemonstrasikan kemampuan Fullstack Development sederhana tanpa database SQL, menggunakan penyimpanan berbasis file JSON (`data.json`) agar ringan dan mudah dipindahkan.

### ✨ Fitur Utama
* **Input Rating & Ulasan:** Pengguna dapat mengirimkan bintang dan komentar.
* **Penyimpanan JSON:** Data tersimpan otomatis di `data.json` tanpa perlu setup database rumit.
* **Flask Framework:** Menggunakan Python Flask untuk backend logic.
* **Responsive UI:** Tampilan rapi menggunakan CSS di folder `static`.

## 🛠️ Struktur File
* `app.py`: File utama (backend) untuk menjalankan server Flask.
* `templates/`: Berisi file HTML (tampilan antarmuka).
* `static/`: Berisi file CSS, JavaScript, dan Gambar.
* `data/` & `data.json`: Tempat penyimpanan data ulasan pengguna.
* `requirements.txt`: Daftar pustaka (library) Python yang dibutuhkan.

## ⚠️ Catatan Penting
Folder `venv` ikut terupload di repository ini. Jika kamu meng-clone repo ini di komputer lain (terutama OS berbeda seperti Windows vs Linux), disarankan untuk membuat virtual environment baru agar tidak terjadi konflik path.

**Author:** Enjeliaa
